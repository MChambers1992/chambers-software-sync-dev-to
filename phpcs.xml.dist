<?xml version="1.0"?>
<ruleset name="Cross Post for Dev.to">
    <description>Coding standards for the Cross Post for Dev.to plugin.</description>

    <file>.</file>
    <exclude-pattern>/vendor/*</exclude-pattern>
    <exclude-pattern>/tests/*</exclude-pattern>
    <exclude-pattern>/coverage/*</exclude-pattern>
    <exclude-pattern>/.phpstan/*</exclude-pattern>

    <arg name="extensions" value="php"/>
    <arg value="sp"/>

    <config name="testVersion" value="7.4-"/>

    <rule ref="WordPress">
        <!-- Short array syntax is used throughout and is intentional. -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <!-- Plugin uses typed properties/return types (PHP 7.4+ baseline). -->
        <exclude name="Squiz.Commenting"/>
    </rule>

    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array" value="cross-post-devto"/>
        </properties>
    </rule>

    <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
        <properties>
            <property name="prefixes" type="array" value="cross_post_devto,Cross_Post_DevTo,CROSS_POST_DEVTO"/>
        </properties>
    </rule>
</ruleset>
